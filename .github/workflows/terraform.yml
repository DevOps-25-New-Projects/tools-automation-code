name: provision tools infra
on: [push, workflow_dispatch]
jobs:
  provision:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@4
      - run: terraform init
      - run: terraform plan






